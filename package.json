{
  "name": "wss-rate",
  "version": "1.0.0",
  "description": "wss middle man rate server",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "./node_modules/typescript/bin/tsc",
    "watch": "pm2 stop dist/server.js --watch && tsc && pm2 start dist/server.js --watch",
    "dev": "pm2 stop dist/server.js && tsc && pm2 start dist/server.js"
  },
  "author": "Danial",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.24.0",
    "cron": "^1.8.2",
    "dotenv": "^10.0.0",
    "express": "^4.17.2",
    "mongodb": "^4.2.2",
    "pm2": "^5.1.2",
    "redis": "^4.0.1",
    "ws": "^8.3.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.13",
    "@types/ws": "^8.2.2",
    "typescript": "^4.5.4"
  }
}
